DEFINE QLOCAL(APP.ORDERS.REQUEST) REPLACE +
  DESCR('Orders request queue') +
  BOQNAME(APP.ORDERS.BACKOUT) +
  BOTHRESH(3)

DEFINE QLOCAL(APP.ORDERS.RESPONSE) REPLACE +
  DESCR('Orders response queue')

DEFINE QLOCAL(APP.ORDERS.BACKOUT) REPLACE +
  DESCR('Orders backout queue')